{
  "private": true,
  "bugs": "https://github.com/nickcis/react-testing-utilities/issues",
  "homepage": "https://github.com/nickcis/react-testing-utilities#readme",
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "format": "lerna run format",
    "lint": "lerna run lint",
    "doctoc": "find . -not -wholename '*node_modules*' -name '*.md' -exec grep -q '<!-- START doctoc' {} \\; -exec doctoc {} \\;"
  },
  "devDependencies": {
    "doctoc": "^1.3.0",
    "lerna": "^2.4.0"
  }
}
